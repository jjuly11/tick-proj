mixin ticket() 
    .ticket
        .ticket_img 
            a(href="#")
                img(src="/images/header_img.jpg", alt="ticket_image")
            hr 
        .ticket_info 
            //- if url.startsWith('/all')
            a(href="#")
                h3 ticket1
            p Testing ticket description 
            p Event Date:
            p Cost:  